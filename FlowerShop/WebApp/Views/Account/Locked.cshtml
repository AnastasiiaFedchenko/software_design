@{
    ViewData["Title"] = "Доступ заблокирован";
}

<div class="row justify-content-center">
    <div class="col-md-6">
        <h2 class="text-center mb-4">Аккаунт заблокирован</h2>
        <p>Вы превысили лимит попыток. Используйте восстановление доступа.</p>

        <div class="alert alert-warning">
            Пользователь: <strong>@ViewBag.UserId</strong>
        </div>

        @if (ViewBag.RecoveryCode != null)
        {
            <div class="alert alert-info">
                Код восстановления (для тестов): <span id="recovery-code">@ViewBag.RecoveryCode</span>
            </div>
        }

        <div class="text-center">
            <a asp-action="Recover" class="btn btn-primary">Восстановить доступ</a>
        </div>
    </div>
</div>
