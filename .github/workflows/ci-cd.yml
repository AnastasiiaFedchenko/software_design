name: .NET CI/CD Pipeline with Allure Reports

on:
  push:
    branches: [ main, lab_test_1 ]
  pull_request:
    branches: [ main, lab_test_1 ]
  workflow_dispatch:

env:
  DOTNET_VERSION: '7.0.x'
  ALLURE_VERSION: '2.24.1'

jobs:
  unit-tests:
    name: Run Unit Tests
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      
    - name: Setup .NET
      uses: actions/setup-dotnet@v3
      with:
        dotnet-version: ${{ env.DOTNET_VERSION }}
        
    - name: Find solution and project files
      run: |
        echo "Solution files:"
        find . -name "*.sln"
        echo "Project files:"
        find . -name "*.csproj"
        echo "Test project files:"
        find . -name "*Tests*.csproj"
      
    - name: Restore dependencies
      run: dotnet restore
      working-directory: ./FlowerShop
      
    - name: Build solution
      run: dotnet build --no-restore --configuration Release
      working-directory: ./FlowerShop
      
    - name: Run unit tests with Allure
      run: |
        dotnet test --filter Category=Unit --configuration Release --no-build --verbosity normal \
        --logger "trx;LogFileName=unit-test-results.trx" \
        --logger "console;verbosity=normal" \
        --results-directory TestResults/Unit
      working-directory: ./FlowerShop
      env:
        ALLURE_RESULTS_DIR: TestResults/Unit/allure-results
      continue-on-error: false
      
    - name: Upload unit test results
      uses: actions/upload-artifact@v4
      if: always()
      with:
        name: unit-test-results
        path: FlowerShop/TestResults/Unit/
        retention-days: 30
        
    - name: Upload Allure results for unit tests
      uses: actions/upload-artifact@v4
      if: always()
      with:
        name: unit-allure-results
        path: FlowerShop/TestResults/Unit/allure-results/
        retention-days: 30

  integration-tests:
    name: Run Integration Tests
    runs-on: ubuntu-latest
    needs: unit-tests
    if: needs.unit-tests.result == 'success'
    
    services:
      postgres:
        image: postgres:15
        env:
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: 5432
          POSTGRES_DB: postgres
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
        ports:
          - 5432:5432

    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      
    - name: Setup .NET
      uses: actions/setup-dotnet@v3
      with:
        dotnet-version: ${{ env.DOTNET_VERSION }}
        
    - name: Install PostgreSQL client
      run: |
        sudo apt-get update
        sudo apt-get install -y postgresql-client
        
    - name: Wait for PostgreSQL
      run: |
        for i in {1..30}; do
          if pg_isready -h localhost -p 5432 -U postgres; then
            echo "PostgreSQL is ready"
            break
          fi
          echo "Waiting for PostgreSQL... ($i/30)"
          sleep 2
        done
        
    - name: Initialize test database
      run: |
        PGPASSWORD=5432 psql -h localhost -U postgres -d postgres -c "CREATE DATABASE flowershoptest;"
        
    - name: Restore dependencies
      run: dotnet restore
      working-directory: ./FlowerShop
      
    - name: Build solution
      run: dotnet build --no-restore --configuration Release
      working-directory: ./FlowerShop
      
    - name: Run integration tests with Allure
      env:
        TEST_CONNECTION_STRING: "Host=localhost;Port=5432;Database=flowershoptest;Username=postgres;Password=5432;Include Error Detail=true"
        ALLURE_RESULTS_DIR: TestResults/Integration/allure-results
      run: |
        dotnet test --filter Category=Integration --configuration Release --no-build --verbosity normal \
        --logger "trx;LogFileName=integration-test-results.trx" \
        --logger "console;verbosity=normal" \
        --results-directory TestResults/Integration
      working-directory: ./FlowerShop
      continue-on-error: false
      
    - name: Upload integration test results
      uses: actions/upload-artifact@v4
      if: always()
      with:
        name: integration-test-results
        path: FlowerShop/TestResults/Integration/
        retention-days: 30
        
    - name: Upload Allure results for integration tests
      uses: actions/upload-artifact@v4
      if: always()
      with:
        name: integration-allure-results
        path: FlowerShop/TestResults/Integration/allure-results/
        retention-days: 30
        
    - name: Cleanup test database
      if: always()
      run: |
        PGPASSWORD=5432 psql -h localhost -U postgres -d postgres -c "SELECT pg_terminate_backend(pid) FROM pg_stat_activity WHERE datname = 'flowershoptest' AND pid <> pg_backend_pid();"
        sleep 2
        PGPASSWORD=5432 psql -h localhost -U postgres -d postgres -c "DROP DATABASE IF EXISTS flowershoptest;"
        echo "Test database cleanup completed"

  e2e-tests:
    name: Run E2E Tests
    runs-on: ubuntu-latest
    needs: integration-tests
    if: needs.integration-tests.result == 'success'
    
    services:
      postgres:
        image: postgres:15
        env:
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: 5432
          POSTGRES_DB: postgres
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
        ports:
          - 5432:5432

    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      
    - name: Setup .NET
      uses: actions/setup-dotnet@v3
      with:
        dotnet-version: ${{ env.DOTNET_VERSION }}
        
    - name: Install PostgreSQL client
      run: |
        sudo apt-get update
        sudo apt-get install -y postgresql-client
        
    - name: Wait for PostgreSQL
      run: |
        for i in {1..30}; do
          if pg_isready -h localhost -p 5432 -U postgres; then
            echo "PostgreSQL is ready"
            break
          fi
          echo "Waiting for PostgreSQL... ($i/30)"
          sleep 2
        done
        
    - name: Initialize test database
      run: |
        PGPASSWORD=5432 psql -h localhost -U postgres -d postgres -c "CREATE DATABASE flowershoptest;"
        
    - name: Build and run application for E2E tests
      run: |
        dotnet build --configuration Release --no-restore
        # Запускаем приложение в фоне для E2E тестов
        dotnet run --project ./FlowerShop --configuration Release --no-build &
        echo "Application starting..."
        sleep 10
      working-directory: ./
      env:
        ASPNETCORE_ENVIRONMENT: Test
        ConnectionStrings__DefaultConnection: "Host=localhost;Port=5432;Database=flowershoptest;Username=postgres;Password=5432"
        
    - name: Wait for application to start
      run: |
        for i in {1..30}; do
          if curl -f http://localhost:5000/health || curl -f http://localhost:5000/api/health || true; then
            echo "Application is ready"
            break
          fi
          echo "Waiting for application... ($i/30)"
          sleep 5
        done
        
    - name: Run E2E tests with Allure
      env:
        TEST_CONNECTION_STRING: "Host=localhost;Port=5432;Database=flowershoptest;Username=postgres;Password=5432;Include Error Detail=true"
        BASE_URL: "http://localhost:5000"
        ALLURE_RESULTS_DIR: TestResults/E2E/allure-results
      run: |
        dotnet test --filter Category=E2E --configuration Release --no-build --verbosity normal \
        --logger "trx;LogFileName=e2e-test-results.trx" \
        --logger "console;verbosity=normal" \
        --results-directory TestResults/E2E
      working-directory: ./FlowerShop
      continue-on-error: false
      
    - name: Stop application
      if: always()
      run: |
        pkill -f "dotnet run" || true
        sleep 3
        
    - name: Upload E2E test results
      uses: actions/upload-artifact@v4
      if: always()
      with:
        name: e2e-test-results
        path: FlowerShop/TestResults/E2E/
        retention-days: 30
        
    - name: Upload Allure results for E2E tests
      uses: actions/upload-artifact@v4
      if: always()
      with:
        name: e2e-allure-results
        path: FlowerShop/TestResults/E2E/allure-results/
        retention-days: 30
        
    - name: Cleanup test database
      if: always()
      run: |
        PGPASSWORD=5432 psql -h localhost -U postgres -d postgres -c "SELECT pg_terminate_backend(pid) FROM pg_stat_activity WHERE datname = 'flowershoptest' AND pid <> pg_backend_pid();"
        sleep 2
        PGPASSWORD=5432 psql -h localhost -U postgres -d postgres -c "DROP DATABASE IF EXISTS flowershoptest;"
        echo "Test database cleanup completed"

  allure-report:
    name: Generate Allure Report
    runs-on: ubuntu-latest
    needs: 
      - unit-tests
      - integration-tests  
      - e2e-tests
    if: always()
    
    permissions:
      pages: write
      id-token: write
      contents: write
      
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      
    - name: Download all test results
      uses: actions/download-artifact@v4
      with:
        path: artifacts
        
    - name: Setup Java for Allure
      uses: actions/setup-java@v3
      with:
        distribution: 'temurin'
        java-version: '11'
        
    - name: Install Allure Commandline
      run: |
        sudo wget https://github.com/allure-framework/allure2/releases/download/${{ env.ALLURE_VERSION }}/allure-${{ env.ALLURE_VERSION }}.tgz
        sudo tar -zxvf allure-${{ env.ALLURE_VERSION }}.tgz -C /opt/
        sudo ln -s /opt/allure-${{ env.ALLURE_VERSION }}/bin/allure /usr/bin/allure
        allure --version
        
    - name: Prepare Allure results
      run: |
        echo "Preparing Allure results..."
        mkdir -p combined-allure-results
        
        # Копируем результаты из всех тестов
        find artifacts -name "*.xml" -exec cp {} combined-allure-results/ \;
        find artifacts -name "*.json" -exec cp {} combined-allure-results/ \;
        find artifacts -name "*.txt" -exec cp {} combined-allure-results/ \;
        find artifacts -name "*.png" -exec cp {} combined-allure-results/ \;
        find artifacts -name "*.jpg" -exec cp {} combined-allure-results/ \;
        
        # Создаем environment файл для Allure
        cat > combined-allure-results/environment.properties << EOF
        OS=Ubuntu Latest
        DotNet.Version=${{ env.DOTNET_VERSION }}
        GitHub.Runner=Ubuntu
        Branch=${{ github.ref }}
        Commit=${{ github.sha }}
        Workflow=${{ github.workflow }}
        Run.Number=${{ github.run_number }}
        EOF
        
        echo "Allure results prepared:"
        ls -la combined-allure-results/
        echo "Total files: $(find combined-allure-results -type f | wc -l)"
        
    - name: Generate Allure Report
      run: |
        allure generate combined-allure-results --clean -o allure-report
        echo "Allure report generated"
        ls -la allure-report/
        
    - name: List Allure report content
      run: |
        echo "=== Allure Report Structure ==="
        find allure-report -type f -name "*.html" | head -10
        echo "=== Total HTML files: ==="
        find allure-report -name "*.html" | wc -l
        
    - name: Upload Allure Report as Artifact
      uses: actions/upload-artifact@v4
      with:
        name: allure-report-full
        path: allure-report/
        retention-days: 30
        
    - name: Setup GitHub Pages
      uses: actions/configure-pages@v3
      
    - name: Upload to GitHub Pages
      uses: actions/upload-pages-artifact@v2
      with:
        path: allure-report/

  deploy-pages:
    name: Deploy Allure Report to GitHub Pages
    runs-on: ubuntu-latest
    needs: allure-report
    if: always()
    
    permissions:
      pages: write
      id-token: write
      contents: read
      
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
      
    steps:
    - name: Deploy to GitHub Pages
      id: deployment
      uses: actions/deploy-pages@v2

  summary:
    name: Test Summary
    runs-on: ubuntu-latest
    needs: 
      - unit-tests
      - integration-tests
      - e2e-tests
      - allure-report
    if: always()
    
    steps:
    - name: Generate Test Summary
      run: |
        echo "## 🧪 Comprehensive Test Report" >> $GITHUB_STEP_SUMMARY
        echo "" >> $GITHUB_STEP_SUMMARY
        
        echo "### 📊 Test Execution Status" >> $GITHUB_STEP_SUMMARY
        echo "" >> $GITHUB_STEP_SUMMARY
        echo "| Test Stage | Status | Details |" >> $GITHUB_STEP_SUMMARY
        echo "|------------|--------|---------|" >> $GITHUB_STEP_SUMMARY
        
        # Unit Tests
        if [[ "${{ needs.unit-tests.result }}" == "success" ]]; then
          echo "| ✅ Unit Tests | ✅ Success | All unit tests passed |" >> $GITHUB_STEP_SUMMARY
        elif [[ "${{ needs.unit-tests.result }}" == "failure" ]]; then
          echo "| ❌ Unit Tests | ❌ Failed | Check unit test results |" >> $GITHUB_STEP_SUMMARY
        else
          echo "| ⚠️ Unit Tests | ❓ ${{ needs.unit-tests.result }} | Unexpected status |" >> $GITHUB_STEP_SUMMARY
        fi
        
        # Integration Tests
        if [[ "${{ needs.integration-tests.result }}" == "success" ]]; then
          echo "| ✅ Integration Tests | ✅ Success | All integration tests passed |" >> $GITHUB_STEP_SUMMARY
        elif [[ "${{ needs.integration-tests.result }}" == "failure" ]]; then
          echo "| ❌ Integration Tests | ❌ Failed | Check integration test results |" >> $GITHUB_STEP_SUMMARY
        elif [[ "${{ needs.integration-tests.result }}" == "skipped" ]]; then
          echo "| ⏭️ Integration Tests | ⏭️ Skipped | Previous job failed |" >> $GITHUB_STEP_SUMMARY
        else
          echo "| ⚠️ Integration Tests | ❓ ${{ needs.integration-tests.result }} | Unexpected status |" >> $GITHUB_STEP_SUMMARY
        fi
        
        # E2E Tests
        if [[ "${{ needs.e2e-tests.result }}" == "success" ]]; then
          echo "| ✅ E2E Tests | ✅ Success | All end-to-end tests passed |" >> $GITHUB_STEP_SUMMARY
        elif [[ "${{ needs.e2e-tests.result }}" == "failure" ]]; then
          echo "| ❌ E2E Tests | ❌ Failed | Check E2E test results |" >> $GITHUB_STEP_SUMMARY
        elif [[ "${{ needs.e2e-tests.result }}" == "skipped" ]]; then
          echo "| ⏭️ E2E Tests | ⏭️ Skipped | Previous job failed |" >> $GITHUB_STEP_SUMMARY
        else
          echo "| ⚠️ E2E Tests | ❓ ${{ needs.e2e-tests.result }} | Unexpected status |" >> $GITHUB_STEP_SUMMARY
        fi
        
        echo "" >> $GITHUB_STEP_SUMMARY
        echo "### 📈 Allure Report" >> $GITHUB_STEP_SUMMARY
        echo "" >> $GITHUB_STEP_SUMMARY
        
        if [[ "${{ needs.allure-report.result }}" == "success" ]]; then
          echo "**🎉 Allure Report Generated Successfully!**" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "📖 **View Report:** ${{ needs.deploy-pages.outputs.page_url }}" >> $GITHUB_STEP_SUMMARY
          echo "📦 **Download Artifact:** allure-report-full" >> $GITHUB_STEP_SUMMARY
        else
          echo "**❌ Allure Report Generation Failed**" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "Check the allure-report job logs for details." >> $GITHUB_STEP_SUMMARY
        fi
        
        echo "" >> $GITHUB_STEP_SUMMARY
        echo "### 🚀 Pipeline Summary" >> $GITHUB_STEP_SUMMARY
        echo "" >> $GITHUB_STEP_SUMMARY
        
        if [[ "${{ needs.unit-tests.result }}" == "success" && "${{ needs.integration-tests.result }}" == "success" && "${{ needs.e2e-tests.result }}" == "success" ]]; then
          echo "**🎊 All tests passed successfully!** The application is ready for deployment. 🚀" >> $GITHUB_STEP_SUMMARY
        else
          echo "**⚠️ Some tests failed or were skipped.** Please review the test results above." >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "**🔧 Next steps:**" >> $GITHUB_STEP_SUMMARY
          echo "1. Check the detailed Allure report" >> $GITHUB_STEP_SUMMARY
          echo "2. Review failed tests and fix the issues" >> $GITHUB_STEP_SUMMARY
          echo "3. Download test artifacts for detailed analysis" >> $GITHUB_STEP_SUMMARY
          echo "4. Re-run the pipeline after fixes" >> $GITHUB_STEP_SUMMARY
        fi
        
        echo "" >> $GITHUB_STEP_SUMMARY
        echo "---" >> $GITHUB_STEP_SUMMARY
        echo "*📋 Report generated automatically by GitHub Actions*" >> $GITHUB_STEP_SUMMARY